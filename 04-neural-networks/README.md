# 04 · Neural Networks

Core building blocks — derived, not just used.

## Contents
- `loss-functions/` — Cross-entropy, focal loss, contrastive loss from probabilistic first principles
- `normalisation/` — BatchNorm vs LayerNorm vs RMSNorm — derivations + comparisons
- `generative-models/` — VAE (ELBO derivation), diffusion basics, GAN minimax
- `regularisation/` — L1/L2 from Bayesian priors, dropout as ensemble
